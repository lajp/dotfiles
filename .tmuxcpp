#!/bin/sh
SESSION=`basename $PWD`

tmux -2 new-session -d -s $SESSION

tmux rename-window -t $SESSION:0 vim
tmux send-keys "nvim ." C-m
tmux new-window -t $SESSION:0 -a -n test

tmux select-window -t $SESSION:0
tmux -2 attach -t $SESSION
